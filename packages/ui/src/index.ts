/**
 * @lh/ui — design system Legal Health (Playbook §2).
 * Criado vazio na Sprint 0 (S0-B); os primitivos (Panel, GoldBtn, Badge,
 * Spinner, SectionTitle, OutputBox) e gráficos migram na Sprint 1.
 */
export const UI_PACKAGE = '@lh/ui';
