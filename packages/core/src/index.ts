/**
 * @lh/core — domínio puro da Legal Health.
 *
 * Esqueleto criado na Sprint 0 (S0-B) com fronteira reservada:
 * este pacote NUNCA importa React, cliente HTTP, SDK de banco,
 * Date.now() ou gerador aleatório (INV-1). A verificação automatizada
 * da fronteira é ativada na tarefa T-04 (S0-I).
 *
 * O motor do Legal Health Index (Spec §7) é extraído para cá na
 * Sprint 1 (B-04). Nenhuma regra de negócio entra aqui na Sprint 0.
 */
export const CORE_PACKAGE = '@lh/core';
